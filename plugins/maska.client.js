// import Maska  from 'maska';

// export default defineNuxtPlugin((nuxtApp) => {
//   nuxtApp.vueApp.directive('maska', Maska); // Регистрируем директиву с именем "maska"
// });

// import Maska from 'maska';

// export default defineNuxtPlugin((nuxtApp) => {
//   nuxtApp.vueApp.directive('maska', {
//     mounted(el, binding) {
//       Maska(el, { mask: binding.value }); // Применяем маску при монтировании компонента
//     },
//     updated(el, binding) {
//       Maska(el, { mask: binding.value }); // Обновляем маску, если значение изменилось
//     },
//   });
// });
